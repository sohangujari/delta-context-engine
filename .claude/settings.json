{
  "mcpServers": {
    "delta": {
      "command": "node",
      "args": ["dist/integrations/claude-code/mcp-server.js"],
      "description": "Delta Context Engine - optimized context per task"
    }
  }
}